# 🚀 Node.js Module System – README

📌 The Node.js module system allows us to split code into smaller reusable modules and use them in a root file using `require()` and `module.exports`.

---

## 📁 Folder Structure

project/
│
├── 📄 app.js  
├── 📄 math.js  
└── 📘 README.md  

---

## 📦 module.exports

👉 `module.exports` is used to **export code** from one file so it can be reused in another file.

📍 Example: Exporting a function from `math.js`.

---

## 📥 require()

👉 `require()` is used to **import exported code** from another file.

📍 The root file `app.js` uses `require()` to access child modules.

---

## 🧠 Module Wrapper Function

👉 Every Node.js file is internally wrapped inside a function:

(function (exports, require, module, __filename, __dirname) {
  // module code
});

✨ Benefits:
- 🔒 Keeps variables private  
- 🧹 Prevents global pollution  
- 📂 Provides `__dirname` & `__filename`

---

## 📦 NPM (Node Package Manager)

👉 NPM is used to:
- 📥 Install packages
- 🧩 Manage dependencies
- ▶ Run scripts

---

## 🔗 Dependency

👉 A dependency is required for the application to run in production.

📌 Example:
npm install express

---

## 🛠 Dev Dependency

👉 A dev dependency is used only during development.

📌 Example:
npm install nodemon --save-dev

---

## 📄 package.json

👉 `package.json` is the **main configuration file** of a Node.js project.

📍 It contains:
- 📛 Project details
- 📦 Dependencies
- ▶ Scripts
- 🔢 Version info

---

## ▶ Scripts in package.json

👉 Scripts allow running commands using npm.

📌 Example:
npm run start

---

## 🔐 package-lock.json

👉 Ensures:
- 🔒 Exact dependency versions
- 🤝 Same setup on all machines
- ⚙ Auto-generated by npm

---

## 🧭 Path Module

👉 The `path` module is used to work with file & folder paths safely.

---

## 🗂 File System (fs) Module

👉 The `fs` module allows:
- 📁 Creating folders
- 📝 Creating files
- ✏ Writing data

---

## 🧪 Complete File System Example (Single File)

const fs = require('fs');
const path = require('path');

📁 Create folder path
const dataFolder = path.join(__dirname, 'Data');

❓ Check if folder exists
if (!fs.existsSync(dataFolder)) {
  fs.mkdirSync(dataFolder);
  console.log('The folder is created');
}

📝 Create file path
const filePath = path.join(dataFolder, 'example.txt');

✍ Write file (Sync)
fs.writeFileSync(filePath, 'Hello Dinesh keep going');
console.log('File created successfully');

⚡ Async file creation
const asyncFilePath = path.join(dataFolder, 'asyncExample.txt');

fs.writeFile(asyncFilePath, 'This file is created asynchronously', (err) => {
  if (err) {
    console.log('Error creating async file');
  } else {
    console.log('Async file created successfully');
  }
});

---

## ⚖ Sync vs Async

🔴 Sync:
- ⛔ Blocks execution
- 👍 Easy to understand

🟢 Async:
- 🚀 Non-blocking
- ✅ Better performance

---

## ✅ Summary

✨ Node.js provides a powerful module system and core modules like `fs` and `path` to build scalable, reusable, and maintainable applications.
